import React from "react";
import { render } from "@testing-library/react";
import TaskList from "../TaskList";

describe("Task tests", () => {
	test("Task correctly displays the name, due date, delete and complete icon", () => {

	});

	test("Clicking the task correctly runs the taskClick function", () => {

	});

	test("Clicking the delete icon correctly runs the deleteTaskIconClick function", () => {

	});

	test("Clicking the complete icon correctly runs the completeTaskIconClick function", () => {

	});

	test("Clicking the complete icon correctly runs the completeTaskIconClick function", () => {

	});

	test("The due date is correctly hidden on mobile", () => {

	});
});